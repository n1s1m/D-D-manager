# Supabase Configuration
# Скопируйте этот файл в .env.local и заполните реальными значениями

# Project URL из Supabase Dashboard -> Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# anon/public key из Supabase Dashboard -> Settings -> API -> API Keys -> anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Required for seeding spells/items (bypasses RLS). Dashboard -> Settings -> API -> service_role (secret)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: for "What would my character do?" in characters app (apifree.ai API key)
# APIFREE_API_KEY=your_apifree_api_key

# Инструкции:
# 1. Перейдите на https://supabase.com
# 2. Создайте проект (или откройте существующий)
# 3. Settings -> API
# 4. Скопируйте Project URL и anon public key
# 5. Вставьте их в этот файл
# 6. Переименуйте файл в .env.local
