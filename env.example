# Supabase Configuration
# Copy this file to .env.local and fill in your values.

# Project URL from Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# anon/public key from Supabase Dashboard → Settings → API → API Keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Optional: for seeding spells/items (bypasses RLS). Dashboard → Settings → API → service_role (secret)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: for "What would my character do?" in characters app (apifree.ai API key)
# APIFREE_API_KEY=your_apifree_api_key

# Instructions:
# 1. Go to https://supabase.com and create or open a project.
# 2. Open Settings → API.
# 3. Copy Project URL and anon public key.
# 4. Paste them above, then save this file as .env.local in the repo root (or in each app directory if running apps separately).
